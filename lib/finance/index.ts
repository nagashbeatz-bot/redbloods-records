/**
 * lib/finance — minimal shared finance helpers (currency-safe aggregation and the
 * received/cancelled classification). Scope is intentionally narrow; see each file.
 */
export * from "./classify";
export * from "./currency";
export * from "./stats";
